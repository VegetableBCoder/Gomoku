[project]
name = "gobang"
version = "0.2.0"
description = "五子棋: 纯逻辑引擎 + 贪心棋手 + KataGo 蒸馏数据训练"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pillow",
]

[project.optional-dependencies]
train = ["torch"]
